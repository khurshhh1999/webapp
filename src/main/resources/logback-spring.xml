<?xml version="1.0" encoding="UTF-8"?>
<configuration debug="true">
    <!-- Set LOG_PATH directly -->
    <property name="LOG_PATH" value="/var/log/myapp" />

    <!-- Console Appender -->
    <appender name="Console" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <!-- Rolling File Appender -->
    <appender name="RollingFile" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_PATH}/application.log</file>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
            <charset>UTF-8</charset>
        </encoder>

        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${LOG_PATH}/archived/application-%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <timeBasedFileNamingAndTriggeringPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedFNATP">
                <maxFileSize>10MB</maxFileSize>
            </timeBasedFileNamingAndTriggeringPolicy>
            <maxHistory>30</maxHistory>
        </rollingPolicy>
    </appender>

    <!-- Root Logger -->
    <root level="INFO">
        <appender-ref ref="RollingFile" />
        <appender-ref ref="Console" />
    </root>

    <!-- Application-specific Loggers -->
    <logger name="controllers" level="TRACE" />
    <logger name="services" level="TRACE" />
    <logger name="aspects" level="TRACE" />
    <logger name="configs" level="TRACE" />

    <!-- AWS SDK Logging -->
    <logger name="com.amazonaws" level="DEBUG" />

    <!-- S3 Client Logging -->
    <logger name="com.amazonaws.services.s3" level="DEBUG" />

    <!-- Hibernate and SQL Logging -->
    <logger name="org.hibernate.SQL" level="DEBUG" />

    <!-- Optionally, adjust Spring and Hibernate logging levels -->
    <logger name="org.springframework" level="INFO" />
    <logger name="org.hibernate" level="INFO" />
</configuration>
